# 4 Set a list of N elements filled with numbers from [-N, N]. Find the product of the elements
# at the specified positions. Positions are stored in the positions list - create this list (for example:
# positions = [1, 3, 6]).